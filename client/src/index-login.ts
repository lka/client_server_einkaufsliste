/**
 * Login page entry point.
 */

import { initLoginPage } from './pages/login.js';

window.addEventListener('DOMContentLoaded', () => {
  // Initialize login page (HTML is already embedded in index.html)
  initLoginPage();
});
